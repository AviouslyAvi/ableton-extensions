.DS_Store
node_modules/
dist/

*.log
*.ablx
*.tsbuildinfo

# …but DO track the compiled, shippable extensions in the deliverable folder.
!dist-extensions/*.ablx

.env

# Extension storage (logs, index.json with sample-library paths, config)
.live-storage/

# Ableton beta SDK binaries — not redistributed (grab from Ableton)
vendor/*.tgz
